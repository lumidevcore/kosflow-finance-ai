[project]
name = "kosflow-finance-ai"
version = "0.9.2"
description = "KosFlow Finance AI - Wasmer compatible pure ASGI backend with local Ollama bridge"
requires-python = ">=3.13"
dependencies = [
    "uvicorn==0.35.0",
    "httpx==0.28.1"
]
